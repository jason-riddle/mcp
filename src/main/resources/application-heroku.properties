# ==============================================================================
# Heroku-specific MCP Server Configuration
# ==============================================================================

# ==============================================================================
# Core Application Configuration for Heroku
# ==============================================================================

# Use PORT environment variable from Heroku
quarkus.http.port=${PORT:8080}

# Bind to all interfaces (required for Heroku)
quarkus.http.host=0.0.0.0

# Application shutdown configuration
quarkus.shutdown.timeout=30s
quarkus.shutdown.delay-enabled=true

# ==============================================================================
# Logging Configuration for Heroku
# ==============================================================================

# Base logging configuration
quarkus.log.level=INFO

# Console logging (required for STDIO transport and Heroku logs)
quarkus.log.console.enable=true
quarkus.log.console.stderr=true

# Disable file logging for Heroku (use Heroku logs instead)
quarkus.log.file.enable=false

# ==============================================================================
# Memory Configuration for Heroku
# ==============================================================================

# Memory persistence configuration - use Heroku ephemeral filesystem
# Note: This will be reset on dyno restart. For persistent storage,
# consider using Heroku Postgres or Redis add-ons
memory.file.path=/app/memory.jsonl

# ==============================================================================
# MCP STDIO Configuration
# ==============================================================================

# Ensure STDIO transport is enabled
quarkus.mcp.server.memory.stdio.enabled=true

# ==============================================================================
# Container Image Configuration (disabled for Heroku)
# ==============================================================================

# Override main application.properties to disable container builds
quarkus.container-image.build=false

# Disable JIB builds completely
%heroku.quarkus.container-image.build=false
